# Exercise 9 - Spring Boot Application

Spring Boot REST API with Spring Web, Spring Data JPA, and H2.

Run:

```bash
mvn spring-boot:run
```

Endpoints:

- `GET /books`
- `GET /books/{id}`
- `POST /books`
- `PUT /books/{id}`
- `DELETE /books/{id}`
